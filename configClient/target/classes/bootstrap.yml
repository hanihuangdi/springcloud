spring:
  cloud:
    config:
      lable: master
      name: cloud-config
      discovery:
         enabled: true
         service-id: configserver
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/